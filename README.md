# pp-game

## Run

In consolas:

- for linux or git bash:

```
virtualenv .venv && pip install -r requirements.txt && .\.venv\Scripts\python .\run.py
```

- for powershell:

```
virtualenv .venv && pip install -r requirements.txt && .\.venv\Scripts\python .\run.py
```